import React from "react";
import { fireEvent, render, screen } from "@testing-library/react";
import App from "./App";

test("renders learn react link", () => {

  // const mockFunction = jest.fn()

  // const { getByTestId, queryAllByTestId } = render(<App buttonHandler={mockFunction} />);
  // // const AppRenderer = render(<App />);
  // const linkElement = screen.getByText(/learn react/i);
  // expect(linkElement).toBeInTheDocument();

  // expect(screen.getByText("Welcome Guest")).toBeInTheDocument();

  // expect(getByTestId("app-container")).toBeInTheDocument();

  // expect(queryAllByTestId("app-container")).toHaveLength(1);
  // // expect(AppRenderer.getByTestId("app-container")).toBeInTheDocument()

  // expect(getByTestId("app-button")).toBeInTheDocument();

  // fireEvent.click(getByTestId("app-button"))
  
  // expect(mockFunction).toBeCalled()

  expect(1).toBe(1)

});
